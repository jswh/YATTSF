{
  "name": "yattsf",
  "version": "0.0.5",
  "description": "yet another toy typescript framework",
  "main": "./build/compiled/app.js",
  "scripts": {
    "build": "tsc",
    "start": "node ./build/compiled/app.js",
    "clean": "rm -Rf dist build",
    "publish": "npm run clean && npm run build && cp ./package.json ./build/compiled/ && cp ./README.md ./build/compiled/ && cd ./build/compiled && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:jswh/YATTSF.git"
  },
  "author": "jswh",
  "license": "GPL-3.0",
  "dependencies": {
    "cookie": "^0.3.1"
  },
  "devDependencies": {
    "@types/node": "0.0.2",
    "typescript": "^2.2.0-dev.20161206",
    "webpack": "^1.14.0",
    "webpack-uglify-js-plugin": "^1.1.9"
  }
}
